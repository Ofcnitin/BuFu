window.BUFU_CONFIG = {
  // Set this to your deployed Cloudflare Worker, e.g.
  // https://bufu-api.<your-subdomain>.workers.dev
  // Until this is set, BUFU shows an honest "not connected" state instead
  // of any placeholder/demo content — there is no offline fixture data.
  apiBase: "",
};
